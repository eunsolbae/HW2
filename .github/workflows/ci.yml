name: CI/CD Pipeline for Personal Color AI Project
on:
  push:
    branches: [ "main" ]
jobs:
  docker-build-push:
    runs-on: [self-hosted, macOS, ARM64]
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
    - name: Deployment Success Simulation
      run: |
        echo "Build and Push process simulated successfully!"
        echo "Deployment complete for Eunsol's Project."